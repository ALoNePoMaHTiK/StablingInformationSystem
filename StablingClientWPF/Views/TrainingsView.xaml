<UserControl x:Class="StablingClientWPF.Views.TrainingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StablingClientWPF.Views"
             xmlns:vm="clr-namespace:StablingClientWPF.ViewModels"
             d:DataContext="{d:DesignInstance vm:TrainingsViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <DatePicker SelectedDate="{Binding CurrentDate}" Height="100"/>
        <StackPanel Orientation="Horizontal" Height="50" HorizontalAlignment="Center">
            <Button Content="Добавить" Command="{Binding OpenCreateTrainingCommand}"/>
            <Button Content="Обновить" Command="{Binding GetTrainingsCommand}"/>
        </StackPanel>
        <ListView x:Name="TrainingsListView" ItemsSource="{Binding Trainings}">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding TrainingId}">№</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding TrainingTypeId}">Тип</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding TrainerId}">Тренер</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding ClientId}">Клиент</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding HorseId}">Лошадь</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Duration}">Длительность</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding TrainingDateTime}">Дата/время</GridViewColumn>
                    <GridViewColumn>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Command="{Binding ElementName=TrainingsListView, Path=DataContext.OpenEditTrainingCommand}"
                                        CommandParameter="{Binding TrainingId}">
                                    <Image Source="/Icons/EditIcon.png" Width="25" Height="25"/>
                                </Button>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>
</UserControl>
